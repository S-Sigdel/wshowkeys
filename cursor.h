#ifndef _WSK_CURSOR_H
#define _WSK_CURSOR_H

/*
 * Generates the "wshowkeys-cursor" Xcursor theme (a pointer inside a gold
 * halo) under the user's icons directory. Returns 0 on success.
 */
int cursor_theme_install(void);

#endif
